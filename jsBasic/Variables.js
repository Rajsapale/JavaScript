/* JS have three types of variables */

var name = "raj";   // it can be redeclered and we can reassign value to it 

name =28;
console.log(name);  // var can be declared globle and can accessed from anywhere from code 

let city="kolhapur"  // let can acceseed within the scope 

const id=2;   // once we declared variable as const then we can't change its value

 // id=4;   this is not allowed when we declared variable as const 
console.log(id);